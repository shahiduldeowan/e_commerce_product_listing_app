name: e_ommerce_product_listing_app
description: "A Flutter implementation of an e-commerce product listing app with pagination, search, and sorting features, following Clean Architecture and BLoC pattern."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
